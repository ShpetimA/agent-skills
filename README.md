# Effect SQL Skill

Portable Effect SQL conventions for handwritten SQL, typed `SqlSchema` queries, transactions, locking, tenant isolation, and migrations.

```sh
npx skills add ShpetimA/agent-skills --skill effect-sql
```

The skill is in [`skills/effect-sql/SKILL.md`](skills/effect-sql/SKILL.md).
